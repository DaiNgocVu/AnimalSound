<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.john.animalsound">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_bee0"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bee0"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimalSound"
        tools:targetApi="31">
        <activity
            android:name=".view.m000_act_splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.m001_act_main" />
        <activity android:name=".view.m002_act_detail" />
        <activity android:name=".view.m003_act_detect_name"/>
    </application>

</manifest>